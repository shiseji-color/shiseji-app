import { createBackgroundStyleImageHandler } from '../../lib/background-style-image-worker.js';

export { createBackgroundStyleImageHandler } from '../../lib/background-style-image-worker.js';

export const handler = createBackgroundStyleImageHandler();
